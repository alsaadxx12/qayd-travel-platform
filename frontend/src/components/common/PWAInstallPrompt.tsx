import React from 'react';

/**
 * PWAInstallPrompt — تم تعطيل النوافذ المنبثقة للتثبيت بناءً على طلب المستخدم.
 */
export const PWAInstallPrompt: React.FC = () => {
  return null;
};

export default PWAInstallPrompt;

