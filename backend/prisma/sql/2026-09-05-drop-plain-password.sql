-- إتلاف كلمات المرور الصريحة: العمود كان يحفظ كلمة كل مستخدم نصاً مقروءاً
-- ويعيدها عبر GET /auth/users. الحذف نهائي وهو المقصود — لا نسخة تُبقى.
-- يُطبَّق بعد نشر الخادم الذي لا يقرأ العمود، فلا ينكسر استعلام قائم.

ALTER TABLE users DROP COLUMN IF EXISTS plain_password;
